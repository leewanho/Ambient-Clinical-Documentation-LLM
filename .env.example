# Copy this file to `.env` (no .example) and fill in your real key.
# `.env` is gitignored; `.env.example` is NOT — never put real keys here.

OPENAI_API_KEY=sk-...

# Optional: only needed for multi-judge cross-validation
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=...
